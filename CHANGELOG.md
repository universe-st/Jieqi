# Changelog

本项目所有版本均出自同一份签名密钥（证书 SHA-256 `b0ec2bcd…89c9`）。
详细验收读数见 [docs/ACCEPTANCE.md](docs/ACCEPTANCE.md)。

## [1.5.2] — 2026-09-15

- **迷雾规则改成「吃王棋」（F4，用户拍板）**：迷雾模式下只有把对方将帅**吃掉**才取胜。
  - **送将合法**：`legalMoves` 在迷雾下直接返回伪合法着法（不做王的安全过滤）；把将帅暴露在攻击下
    是合法的（代价是可能被吃）。场景里送将的红 X 与拦截在迷雾下消失。
  - **将死/困毙不再判负**：`computeResult` 在迷雾下只看「将帅被吃」与「久无吃子判和」两件事；
    被将死的一方因送将合法永远有着法可走，游戏必然下到将帅被真正吃掉为止。
  - **AI 同规则**：搜索的采样世界同样跳过王的安全过滤，吃掉将帅在搜索里是即时必胜终局。
- **将军提示以「将军的棋」为门（F5，用户拍板）**：被将军时，除非将军的棋至少一枚落在玩家视野里，
  否则不提示（无高亮/状态栏/横幅）—— 雾里的将军不泄密。
- **迷雾回退柔雾样式**（用户拍板）：雾格瓦片回到半透明柔雾（30 层渐变 + 呼吸 0.8↔0.96 + 飘动雾团），
  1.5.1 的实墙作废；「棋子不画」保留 —— `BoardView.reconcile` 直接 `setVisible(false)` 雾格上的棋子，
  雾下棋子不可能透出来。
- 测试：`test/vision.test.ts` 新增 F4 一组 4 例（送将合法、吃将即胜、将死局面不判负、对照标准模式仍判
  将死）；typecheck 0 错、`121/121` 全绿。
- 浏览器实测（headless）：迷雾对局跑通 5+ 手，雾格 39 格半透明覆盖、雾下棋子不显示、电脑落子后状态栏
  必现「对方已落子（迷雾中，看不清具体走法）」，全程 0 运行时错误。
- 签名 release 包：`jieqi-1.5.2-release.apk`（versionCode **19**，V2 签名同密钥 `b0ec2bcd…89c9`）。

## [1.5.1] — 2026-09-15

- **迷雾玩法三处修正**（用户拍板）：
  - **F3 敌王按「最后一次看见的位置」追踪**：`JieqiGame.kingSeen` 记录各观察者上次见到敌方将帅的
    格子（默认敌王原位），每手按视野刷新；AI 的迷雾棋盘把敌王摆在 `kingSeen` 而不是真位置 ——
    把王藏进迷雾真的藏得住了，代价是 AI 会对着幻影王走棋（被真盘拒绝就顺延）。取代 1.5.0 的
    「敌方将帅位置恒已知」例外。玩家侧视野棋盘不受影响。
  - **迷雾变实墙**：雾格瓦片内部全不透明，且 `BoardView.reconcile` 直接把雾格上的棋子视图
    `setVisible(false)`（不是盖住、是不画）—— 雾下棋子不可能再透出来。雾的动感由飘动雾团承担。
  - **「对方已落子」提示**：电脑在迷雾中落子时状态栏必现「对方已落子（迷雾中，看不清具体走法）」；
    最坏情况下（视野信息完全没变）这行字是玩家知道对手走了一手的唯一信号。
- 测试：`test/vision.test.ts` 新增 kingSeen 追踪不变量、敌王接管位置、占位不覆盖 3 例；
  typecheck 0 错、`117/117` 全绿。
- 签名 release 包：`jieqi-1.5.1-release.apk`（versionCode **18**，V2 签名同密钥 `b0ec2bcd…89c9`）。

## [1.5.0] — 2026-09-15

- **新增第三种玩法「迷雾玩法」**：标准玩法 + 视野系统（用户拍板的设计）。
  - **视野（V1–V3）**：一枚棋子的视野 = 自身所在格 + 周围八格 + 一步之内能走到的所有格；
    炮能看到隔山打的那枚子但看不到炮架；暗子按所在格位算视野，翻开后按真身算；
    玩家只能看到己方棋子看得见的格子，其余被迷雾粒子效果覆盖（每格迷雾瓦片 + 飘动的雾团）。
  - **将帅碰头（F1–F2）**：连线被无视野区域挡住时不算照面；迷雾驱散导致照面时，
    轮到的一方可直接飞将吃掉对面将帅立即获胜。
  - **吃子提示/送将/落点危险全部在玩家的视野棋盘上计算**，绝不提示玩家看不到的信息；
    将军提示同样按可见性门控；迷雾里电脑行棋的记谱与翻子信息不进入棋谱（玩家没看见就不写）。
  - **AI 同样受视野限制**（用户拍板）：候选着法在 AI 的视野棋盘上生成，走子按真实规则校验、
    被挡顺延次优步；搜索世界里看不见的敌子按采样位置重摆（唯一例外：敌方将帅位置恒已知）。
  - 实现：`src/core/vision.ts`（视野计算 + Zobrist key 全局缓存 + foggedBoardFor）、
    `moves.ts` 的 `kingsFaceEachOtherFog` 与飞将吃将、`ai/engine.ts` 的 `fogCandidates` 与
    `sampleFogWorld`、`BoardView` 迷雾层、`GameScene` 可见性门控。
- 测试：新增 `test/vision.test.ts`（16 例：视野规则、迷雾照面、飞将吃将、迷雾 AI），
  typecheck 0 错、`114/114` 全绿。
- 签名 release 包：`jieqi-1.5.0-release.apk`（versionCode **17**，V2 签名同密钥 `b0ec2bcd…89c9`）。

## [1.4.3] — 2026-09-15

- **棋子文字全面转繁体**：`KIND_NAME` 的繁简异形四子改为 帥/將/馬/車（炮兵卒仕士相象繁简同形，不变）。
  抽子判词同步（红 帥 / 黑 將）；棋谱记法跟着统一繁体（車九進五、暗兵五進一、前/後），避免半繁半简。
  字体霞鹜文楷本就带全繁简字形（WOFF2 cmap 已核，无需换字体）。
- **界面文案（楚河汉界、菜单、提示语）保持简体**——本次只改棋子字。
- 测试与构建：typecheck 0 错、`97/97` 全绿（5 处记法/吃子断言更新为繁体预期）。
- 顺带修正：`src/debug/backdoor.ts` 后门版本号停在 1.4.1 已两版，本次同步到 1.4.3。
- 签名 release 包：`jieqi-1.4.3-release.apk`（versionCode **16**，**17,875,959 B**，V2 签名同密钥 `b0ec2bcd…89c9`）。

## [1.4.2] — 2026-09-15

- **全游戏不再有焦点框**：`MVVMPlugin.configure({ focus: { ring: false } })`。揭棋是手指玩的游戏，框架的焦点环
  （"键盘在这儿"的提示）在本作里没有受众——它过去会在对话框自动聚焦第一个控件时套在滑杆上。关掉的只是
  "画不画"：`Tab`/方向键照旧移动焦点、`Enter` 照旧激活、无障碍镜像照旧报告聚焦的控件，删掉这一行即可恢复
  （见 [docs/ACCEPTANCE.md](docs/ACCEPTANCE.md) §13.4）。
- **同步 phaser-mvvm 基线**（`pnpm vendor:mvvm`，上游 `ef2b43d`）：这次集成顺带在框架侧修掉三条缺陷——点过/
  被聚焦过的控件不再留框（`:focus-visible` 语义）、`focus.ring` 从只写不读的死选项变成真开关、滑杆的重绘缓存键
  补上可见性。本地 Slider 补丁仍在。完整来龙去脉见 [docs/ACCEPTANCE.md](docs/ACCEPTANCE.md) §13。
- 测试与构建：`97/97` 全绿、typecheck 0 错、`vite build` 通过；框架侧的三次改动一条都没碰到规则引擎与 AI。
- 签名 release 包：`jieqi-1.4.2-release.apk`（versionCode **15**，**17,875,959 B**，V2 签名同密钥 `b0ec2bcd…89c9`）。

## [1.4.1] — 2026-09-14

- **禁止循环追棋**（取代禁止全局同形）：同一局面**最多出现两次**，第三次重现的着法才禁止
  （`wouldRepeat` 判 `repetitionCount(key) >= 2`）；**走子方正被将军时完全豁免**
  （`wouldRepeat` 对 `inCheck()` 直接放行）。全部提示文案改为「禁止循环追棋」。
- **混斗新增禁止立即吃将**：翻出的敌方子若照着自己将帅（`selfCheck`），对方**下一手不能用这枚子
  直接吃将**（`wouldEatGeneral`），被将军一方得一回合解将；走完那一手规则即失效。
  AI 根着法表（`selectableMoves`）过滤、`apply()` 兜底抛错、UI 横幅「禁止立即吃将」+ 状态栏计数。
- 测试：`rules.test.ts` 重构为第三次重现判禁 + 被将军豁免 + AI 不选禁着；`mixed.test.ts` 改为
  禁吃将 + 规则一手后失效。**97/97 全绿**。
- 签名 release 包：`jieqi-1.4.1-release.apk`（versionCode 14，17,875,627 B，V2 签名同密钥）。
  坚果云：`揭棋/揭棋_20260914_v7.apk`。

## [1.4.0] — 2026-09-14

- **新增玩法：混斗**（开始游戏时在「玩法」弹窗里选，选择记在 `jieqi.mode.v1`）。除将帅外，
  红黑 30 枚棋子**混洗成一池**、逐格背面朝上；**己方半场的暗子暂时归自己**（可走、可吃、算己方保护），
  暗子走法仍按格位（R4 不变）；翻开若是敌方棋子，**该子当场易主**，停在落点上、之后由对方使用。
  - 合法性**不因暗子身份而变**：判据仍是"这一步会不会送将"（新增 `isKingSafeAfter()`，
    假设该子仍属走子方）。翻出敌方子反将自身的后果由下一步承担（`MoveEvent.selfCheck`）；
    将帅被吃后按"无将即被攻击"判将死（`isKingSafe` 增加一条）。
  - 归属权统一走 `Board.ownerAt()`：暗子看半场、明子看真身，走法生成/攻击检测/吃子提示/记谱全部改用它。
  - 信息池改为 **30 枚一池**（`mixedPoolFor` / `sampleWorldMixed`），AI 的 PIMC 连"这枚暗子是谁的"一起抽。
  - 被吃子托盘在混斗里按**吃子方**归档（`mixedTrayChips`），计数改为"吃子 N 枚"；记谱按**走子方**读，
    事件另带 `revealedColor`。
  - 界面：玩法弹窗、棋盘顶栏徽标、`翻出敌方X · 易主` 状态行、横幅「易主」/「反将」。
  - 新增 `test/mixed.test.ts` 26 条（发子、半场归属、易主、反将与被吃将、信息池守恒、混斗托盘、AI）。
  - 签名 release 包：`jieqi-1.4.0-release.apk`（versionCode 13，17,875,355 B，V2 签名同密钥）。

## [1.3.2] — 2026-09-14

- **全部合法着法都重复时按困毙判负**（不再判和）：`computeResult` 的
  `selectableMoves` 空分支由 `循环重复，判和` 改为 `stalemate` 困毙判负
  （winner=对方）。用户拍板；旧串已从包内清除。

## [1.3.1] — 2026-09-14

- **定先后界面切换对局 BGM**：DrawScene 从 menu 曲改 game 曲（定先后即视为
  对局开始；GameScene 同状态 no-op 无缝延续）。

## [1.3.0] — 2026-09-14

- **BGM 状态管理器**：`AudioDirector.setBgm` 按状态切歌（menu/game/win/lose），
  新增三首对局曲入库。
- **吃子提示红绿分级**：选中棋子后会被白吃的目标格改画红圈（复用 `isHanging`），
  安全格保持绿圈。
- **送将提示**：移动会暴露己方将帅的格画红叉（与吃子提示开关无关），点击弹
  「移动会送将」警告。

## [1.2.4] — 2026-09-14

- **恢复锐利渲染 + 柔和翻面揭示光**：还原设备分辨率渲染（回到 1.2.2 观感）；
  刺眼的元凶是落子翻面的揭示闪光（`PieceView.sweep` / `TEX.glow`），已调柔和
  —— 尺寸 `PIECE_RADIUS×2.8→2.0`、初始 alpha `0.9→0.5`、最大膨胀 `1.6→1.3`。

## [1.2.3] — 2026-09-14

- 还原 1.2.2 的设备分辨率 pass（锐利刺眼），回到设计分辨率渲染
  （`RENDER_SCALE=1` / `BAKE_SCALE=2`）。

## [1.2.2] — 2026-09-14

- **设备分辨率渲染（锐利版）**：缓冲=设计×dpr、相机缩放。

## [1.2.1] — 2026-09-14

- 设计分辨率渲染基线。

## [1.2.0]

- **吃子提示**：难度设置里的「吃子提示」复选框，开出来的红/绿光圈
  （三条判据见 README §R13）。
- **禁止全局同形**：任何一方都不得使局面回到本局出现过的样子（替代长将/长捉
  的"第三次重复"口径；Zobrist 键判据）。

## [1.1.0]

- **开始界面**：开始游戏 / 难度设置 / 音量设置。
- **定先后抽子动画**：棋子正反面决定玩家执红还是执黑，玩家颜色从此可变。
- **音效系统**：抽子等音效入库（15 个音频）。
- 1.1.0 第二轮构建：游戏内字体换成**霞鹜文楷**、新增**加载页**、对局结束时
  **终局揭示**（暗子全部翻开并压半透明）。

## [1.0.0]

- 首个发布：揭棋（中国象棋变种）人机对战，规则引擎 + PIMC 信息集 AI +
  竖屏中国古风画面，通过 Cordova 打包为签名 Android APK。
