# 揭棋 · JieQi — 实施计划

> 目标：用 **Phaser 4 + Cordova** 写一个与 AI 对战的**揭棋**（中国象棋变种）游戏。
> 竖屏手机画面，中国古风画风，UI 用 **phaser-mvvm**，AI 有智商但保留一丝随机性，
> 根目录提供签名 release APK 的构建脚本。
>
> 本文件是**动手前**写下的计划（需求 1）；实现过程中若与计划冲突，先改本文件再改代码。

---

## 0. 规则基线（已取证，不是猜的）

揭棋的规则有两个权威来源，二者一致：

- QQ 象棋官方《揭棋玩法说明》— <https://image.qqchess.qq.com/test_160324/activity/html/jieqiguize.html>
- 维基百科「揭棋」— <https://zh.wikipedia.org/wiki/揭棋>

| #   | 规则                                 | 出处                                        |
| --- | ------------------------------------ | ------------------------------------------- |
| R1  | 棋盘棋子同中国象棋（9 列 × 10 行）   | 维基「棋盘与棋子」                          |
| R2  | 帅/将**明子**摆在九宫原点           | 维基「摆子」/ QQ「将、帅放在原点」          |
| R3  | 其余各 15 枚为**暗子**随机摆放       | 维基「摆子」/ QQ「各15个暗子随机摆放」      |
| R4  | **暗子按其所在格位走子**：在哪个棋子的初始格位上，就按该棋子的走法走子、吃子 | 维基「暗子走法」；QQ「按其所在位置照中国象棋规则走」 |
| R5  | 暗子走完**必须翻开**，原有暗子走法作废 | 维基「暗子走法」                            |
| R6  | 暗子**可以吃子，也可以被吃**         | QQ 三.2 / 维基                              |
| R7  | 暗子被吃后**背面朝上**放置，**被吃方不能翻看** | 维基「暗子被吃后…被吃子的一方不能翻看」；QQ 三.3 |
| R8  | 明子走法同中国象棋，**唯一区别：相（象）与仕（士）允许过河** | 维基「明子走法」/ QQ 四.2           |
| R9  | **暗子**的仕/士**不能**出九宫（与象棋完全相同） | 维基例 2-3                          |
| R10 | 胜负：将死、困毙、长打、认输          | 维基「胜负判断」/ QQ 六                     |
| R11 | 长捉 / 长将：同一棋子连续捉/将超 6 回合后禁着 | QQ 五.1、五.2                        |
| R12 | 空步判和：双方无吃子达 40 回合判和    | QQ 五.3                                     |

**由 R4/R8/R9 推出的完整走子模型**（这是引擎的核心，写死在 `src/core/moves.ts`）：

```
走子规则 = f(棋子当前状态)
  ├─ 暗子：按「该格位在中国象棋初始局面里对应的棋子类型」走，且受该类型的全部限制
  │        （暗仕/暗士不能出九宫；暗相/暗象不能过河；暗兵/暗卒未过河只能向前）
  └─ 明子：按「它真实的身份」走，且相/象、仕/士 解除过河与九宫限制
```

推论（必须写进单测）：

- **暗子的走法与其真实身份无关**，所以"合法着法"是完全确定的，运气只体现在**翻开结果**上。
- 一个格位的暗子可能翻开成任意身份 —— 例如「暗车格位」上的子走完可能翻出「兵」。
- 被吃的暗子**不翻开**，因此该身份的归属对双方永久未知（`InfoState` 的核心）。

---

## 1. 技术基线

| 项         | 选择                                                  | 理由                                                       |
| ---------- | ----------------------------------------------------- | ---------------------------------------------------------- |
| 引擎       | **Phaser 4.2.1**（`phaser@4.2.1`）                    | 需求指定                                                   |
| UI 框架    | **phaser-mvvm 1.0.0**（`core`/`layout`/`phaser`/`widgets`） | 需求指定；以 workspace 协议引用**源码**               |
| 语言/构建  | TypeScript strict + Vite                               | 与 phaser-mvvm 一致                                        |
| 测试       | vitest（纯 Node，核心与 AI 零渲染依赖）                | 规则引擎必须可证                                           |
| 打包       | Cordova（`cordova-android`）+ 签名 release             | 需求指定                                                   |
| 画面       | 竖屏设计分辨率 **450 × 900**，`Phaser.Scale.FIT`       | 竖屏手机；设计分辨率让布局在任何机型上一致                 |

**依赖引入方式**：phaser-mvvm 的四个包**尚未发布到 npm**，且本仓库不能假设兄弟目录存在。
因此把四个包的 `src` **vendored** 到 `vendor/phaser-mvvm/packages/*`，并在 `pnpm-workspace.yaml`
里把它们纳入 workspace，应用侧用 `"@phaser-mvvm/core": "workspace:*"` 引用。
好处：仓库自包含、离线可构建、不依赖 `/Users/kuangshensheng/codes/phaser-mvvm` 这个外部路径。

---

## 2. 架构

```
src/
├─ core/          ── 纯逻辑，零 Phaser、零 DOM，可在 Node 单测
│  ├─ types.ts       棋子/颜色/坐标/身份；格位类型表
│  ├─ board.ts       Board 数据结构（10×9 扁平数组）、复制、Zobrist 哈希
│  ├─ moves.ts       伪合法着法生成（暗子按格位 / 明子按身份）
│  ├─ rules.ts       合法性过滤、将军检测、将死/困毙、长将长捉、空步判和
│  ├─ setup.ts       随机摆子（R2/R3）
│  ├─ info.ts        信息集：每方"未翻开身份池"、determinization 采样
│  └─ notation.ts    中文记谱（炮二平五）与 ICCS 坐标，用于 UI 与调试
├─ ai/            ── 纯逻辑
│  ├─ evaluate.ts    子力 + 位置表 + 机动性 + 将安全（揭棋补充项）
│  ├─ search.ts      alpha-beta + 迭代加深 + 置换表 + 杀手/历史 + 静态搜索
│  ├─ pmc.ts         完全信息蒙特卡洛：按信息集采样 N 个世界，各自搜索后平均
│  └─ engine.ts      对外 API：chooseMove(info, opts) → { from, to, score, stats }
├─ ui/            ── Phaser + phaser-mvvm
│  ├─ theme.ts       中国古风主题（自定义 Theme 对象）＋ 画风调色板
│  ├─ textures.ts    程序化生成的木纹/云纹/棋子贴图（无外部美术资源）
│  ├─ BoardView.ts   棋盘：原生 Phaser Graphics + 棋子 Container
│  ├─ PieceView.ts   单枚棋子：正面/背面两套外观 + 翻转
│  └─ fx.ts          动画库：发牌、翻转、走子、吃子、将军、终局
├─ vm/
│  └─ GameViewModel.ts  MVVM ViewModel（ref/computed），驱动 phaser-mvvm 的 HUD
├─ scenes/
│  ├─ BootScene.ts   生成贴图
│  └─ GameScene.ts   棋盘 + MVVM UI + 输入编排
├─ debug/backdoor.ts  window.__JIEQI__ 验收后门
└─ main.ts
test/               core 与 ai 的单测
```

**分层铁律**：`core`/`ai` 不允许出现 `import Phaser` 或 `window`；`ui`/`scenes` 才碰渲染。
这条与 phaser-mvvm 自己的 ADR-0003 一致，也是"规则可测"的前提。

### 2.1 为什么棋盘用原生 Phaser、HUD 用 phaser-mvvm

phaser-mvvm 的定位就是「UI 归 MVVM，游戏逻辑照常用原生 Phaser」。棋盘是有 90 个可交互格位、
需要逐帧补间与粒子的**游戏对象**，塞进自动布局引擎只会打架。
所以：

- **棋盘/棋子**：原生 `Graphics`/`Container` + `scene.tweens`；
- **HUD**（双方信息栏、被吃子托盘、状态文本、按钮、设置/结算对话框）：**phaser-mvvm Compose DSL**，
  数据由 `GameViewModel` 的 `ref`/`computed` 驱动，换肤走 `setTheme`。

### 2.2 输入与相机的两个坑（phaser-mvvm ADR-0009 已记录）

1. MVVM 的页面默认 `blockPointer: true`，会吞掉棋盘点击 → 页面根必须 `blockPointer: false`，
   只有真正的面板/按钮才拦截指针。
2. UI 用 `page.setScrollFactor(0)` 钉住；相机不动，但保持这个约定可让 HUD 将来能跟随镜头。

---

## 3. AI 设计（需求 4：有智商 + 一丝较小随机性）

### 3.1 问题：这是**不完全信息**博弈

双方都看不到任何暗子的身份（包括自己的 —— 见 R7 推论）。
不能用普通 minimax 直接搜，因为"状态"对玩家不是一个点，而是一个**信息集**。

### 3.2 方案：PIMC（完全信息蒙特卡洛）+ alpha-beta，外加微小噪声

```
chooseMove(info) :
  worlds = sample(info, N)            # 按"未翻开身份池"随机给每个暗子格位派一个身份
  for each w in worlds:
      score[w] = alphaBeta(w, depth)  # 在确定性世界里普通搜索
  avg[move] = mean over w of score[w][move]
  avg[move] += noise(move)            # ← 一丝随机性（±noiseCp 厘兵）
  return argmax avg
```

**信息集的表示**（`core/info.ts`）：每方身份池 = 该方 15 枚原始身份 − 已翻开的身份；
池中剩余的 |pool| 枚身份，分配给"仍在盘上的暗子格位"与"已被吃的暗子（未知，出局）"。
这正是 R7 的直接后果，也是本作与普通象棋 AI 最本质的区别。

### 3.3 评估函数

子力（车 900 / 炮 450 / 马 400 / 相仕 200 / 兵 100，过河兵 200）+ 位置表 +
机动性 + 将安全 + 车占开放线 + 炮架。**不**额外给暗子特殊分：暗子的不确定性
已经由 PIMC 对多个世界求期望处理掉了。

### 3.4 随机性的三个来源（都"较小"）

1. 世界采样本身带来的期望差 → 天然多样化；
2. 根节点每个着法加 `±noiseCp`（默认 6 厘兵）均匀噪声；
3. 以 `epsilon`（默认 5%）概率在"落后最优不超过 `slack`（默认 25 厘兵）"的着法里随机挑一个。

三者都可由难度档位调（`easy`/`normal`/`hard`），并接受固定种子 → **可复现**（验收需要）。

### 3.5 性能预算

节点预算硬上限（默认 240k 节点），迭代加深 + 置换表 + MVV-LVA 排序 + 静态搜索。
揭棋分支因子低（约 20–35），目标：中端手机 < 1s，桌面 < 300ms。
搜索跑在主线程之外不可行（Cordova 下 worker 风险大），因此用**节点预算**而非时间来保证不卡死，
且 AI 思考期间 UI 播放"思考中"动画（同步搜索会短暂卡住渲染，用预算把它压在可接受区间）。

---

## 4. 动画清单（需求 3）

| #   | 动画         | 内容                                                                 |
| --- | ------------ | -------------------------------------------------------------------- |
| A1  | 开局发牌     | 32 枚棋子从屏幕两侧交错飞入并落位，暗子带旋转与落地弹性               |
| A2  | 选子         | 棋子抬起 + 阴影分离 + 呼吸浮动；合法落点出现脉动光圈                 |
| A3  | 翻转揭子     | `scaleX` 1→0→1 的"翻面"，中点切换正反面，配一道扫光与光晕扩散        |
| A4  | 走子         | 沿路径带轻微抛物线位移 + 影子跟随 + 落子回弹 + 尘土粒子              |
| A5  | 吃子         | 被吃子旋转、缩小、淡出；冲击波环 + 木屑粒子；吃子方轻微顿帧          |
| A6  | 将军         | 将/帅红光脉冲 + 屏幕边缘血色渐晕 + 「将军」横幅弹入                  |
| A7  | 终局         | 「胜/负/和」大字横幅 + 花瓣飘落 + 按钮淡入                           |
| A8  | 提示/悔棋    | 提示着法箭头绘制动画；悔棋时棋子沿原路飞回                           |
| A9  | AI 思考      | 顶部太极/罗盘旋转 + 呼吸光点                                         |
| A10 | 回合切换     | 状态条滑动切换 + 当前方信息栏高亮过渡                                |
| A11 | UI 进出      | 对话框/面板走 phaser-mvvm 的 `transition` 令牌                       |

---

## 5. 验收（需求：用 playwright 玩一局）

### 5.1 后门（`window.__JIEQI__`）

验收要"玩一局"，但逐格点击 32 枚互不知情的暗子既慢又脆。按需求允许留后门：

| API                                | 用途                                              |
| ---------------------------------- | ------------------------------------------------- |
| `state()`                          | 完整快照：棋盘、回合、状态、步数、信息池          |
| `legalMoves()`                     | 当前合法着法（`from`/`to`/记谱）                  |
| `point(square)`                    | 某格位的**画布坐标**（用于真实鼠标点击）          |
| `click(square)` / `clickMove(f,t)` | 真实指针路径：`mouse.move/down/up` 等价           |
| `move(from, to)`                   | 程序化走子（等动画结束）                          |
| `autoPlay(n?)`                     | 双方 AI 自动下完整局，返回结果                    |
| `settle()`                         | 等所有补间/动画停止                               |
| `newGame(seed)`                    | 可复现开局                                        |
| `errors()`                         | 收集的运行时错误（**验收要求 0 条**）             |

### 5.2 验收矩阵

1. 真实鼠标点击选子 → 高亮 → 点击落点 → 走子/翻转动画 → 回合切换；
2. `autoPlay()` 跑完整局（红黑双方均由 AI 驱动）直至分出胜负或和棋，过程中 `errors()` 必须为空；
3. 动画全部 settle 后棋盘状态与引擎状态一致；
4. 悔棋、认输、新局按钮各点一次；
5. 竖屏截图确认画风与布局无重叠/越界。

---

## 6. Android 签名 release（需求 5）

根目录脚本 **`build-android-release.sh`**：

1. `pnpm install`（必要时）→ `pnpm build` 产出 `dist/`；
2. `cordova/` 工程（`com.jieqi.game`）的 `www/` 用 `dist/` 覆盖；
3. 若 `cordova/keystore/jieqi-release.keystore` 不存在则用 `keytool` 生成（RSA 2048 / 10000 天）；
4. `cordova build android --release -- --keystore … --alias … --storePassword … --password …`；
5. 校验产物：`apksigner verify --print-certs`，并把 APK 复制到 `release/jieqi-<版本>.apk`。

凭据集中在脚本顶部的变量里，可用环境变量覆盖（`JIEQI_KEYSTORE_PASSWORD` 等）。
**keystore 与口令一并提交到仓库**是为了让脚本开箱即用；文件头会用注释写明这是**演示用证书**，
正式发布请换成自己的密钥库。

JDK：脚本自动探测 `JAVA_HOME` → Homebrew `openjdk@21` → Android Studio 内置 JBR。

---

## 7. 里程碑

| M   | 内容                                                    | 完成判据                          |
| --- | ------------------------------------------------------- | --------------------------------- |
| M0  | 计划（本文件）+ 工程骨架 + vendored phaser-mvvm 能 `dev` | `pnpm dev` 起得来、空场景能渲染    |
| M1  | 规则引擎 `core/` + 单测                                  | vitest 全绿，覆盖 R1–R9            |
| M2  | AI `ai/` + 自对弈冒烟                                    | 自对弈 100 局无非法着法、无异常    |
| M3  | 棋盘渲染 + 古风贴图 + 动画                                | 竖屏截图可看                       |
| M4  | HUD（phaser-mvvm）+ ViewModel + 输入编排                  | 真实点击能走完一步                 |
| M5  | 后门 + Playwright 验收                                   | 验收矩阵 5 条全过                  |
| M6  | Cordova 工程 + 签名 + release 脚本                        | `build-android-release.sh` 产出签名 APK |
| M7  | README + 收尾                                            | 文档与实现一致                     |

---

## 8. 已识别的风险与对策

| 风险                                       | 对策                                                          |
| ------------------------------------------ | ------------------------------------------------------------- |
| phaser-mvvm 未发布，跨仓库引用易碎         | vendored 到本仓库并纳入 workspace（§1）                        |
| 同步 AI 搜索卡住渲染                       | 节点预算硬上限 + 迭代加深，先量再定档                          |
| 暗子走法被误解成"按身份走"                 | 规则基线写进 PLAN §0 与单测，R4/R9 各一条用例                  |
| 被吃暗子的身份未知 → 信息池状态空间        | `InfoState` 显式建模，单测断言"池总和守恒"                     |
| Cordova 构建需要 JDK/SDK，环境可能缺失     | 脚本自动探测并给出可执行的报错；本机已验证 JDK 21 + SDK 36 存在 |
| 长将/长捉规则复杂                          | 简化实现：重复局面 3 次 + 连续将军判定，写进 `rules.ts` 并有单测 |
